# DESIGN.md — 传感器数据大屏

> 设计方向依据 Anthropic `frontend-design` skill 重构（见 `.trae/skills/frontend-design/SKILL.md`）。

## 主题与任务

- **主题**：工业窑炉控制室 —— 深夜值守的窑炉中控台，大屏上数据如炉火般跳动。
- **受众**：值班工程师 / 技术员，需在数秒内判断全局健康状态。
- **单一任务（thesis）**：一屏掌握「在线多少、有无告警、趋势如何」——大数字即论点。

## 视觉签名（signature）

- **顶部「信号迹线」**：视口最上方一条极细的渐变信号带（传感器实时信号的隐喻），缓慢脉动，是全屏唯一的装饰性动效。
- 其余元素保持克制：面板用精密「蚀刻」边框 + 顶部语义色强调线，颜色只服务于语义，不用于装饰。

## 配色（token）

| Token | 值 | 用途 |
|-------|----|----|
| `--background` | `#0b0e13` | 画布（冷黑蓝，非纯黑） |
| `--card` | `#14181f` | 面板底 |
| `--card-hover` | `#1a1f28` | 悬停 |
| `--foreground` | `#e8edf4` | 主文字 |
| `--muted-foreground` | `#8b96a6` | 次要文字 |
| `--primary` | `#f5a524` | 烬金（炉火主题，签名强调） |
| `--success` | `#38c172` | 正常 / 在线 |
| `--warning` | `#f7c948` | 预警 |
| `--danger` | `#ff4d5e` | 超限 / 离线 |
| `--info` | `#4da3ff` | 信息 / 数据 |
| `--border` | `rgba(155,170,192,0.14)` | 蚀刻边框 |

## 字体排版

- 数据 / 数字：`JetBrains Mono`（tabular-nums，大数字 700）。
- 中文 / 正文：系统无衬线 `PingFang SC / Microsoft YaHei`。
- 层级：面板标题 13px/600；大数字 28–32px/700；表格 13px 等宽。

## 动效

- 微交互 150ms、标准 300ms、入场 500ms（ease-out）。
- 入场：面板首屏 `fade-up` 揭示（仅一次，非滚动触发）。
- 数字补间：`easeOutCubic` 500ms。
- 全部动效尊重 `prefers-reduced-motion: reduce`。

## 设计禁忌

- 不用亮白 / 浅色主题。
- 不用花哨渐变、霓虹发光、大圆角（保持工业精密感）。
- 颜色只用于语义（状态 / 告警），不做无意义着色。
- 不让骨架屏 / 空态 / 错误态退化成「暂无数据」一行字。
